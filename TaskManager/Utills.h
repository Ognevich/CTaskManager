#ifndef UTILLS_H
#define UTILLS_H
#include <stdlib.h>
#include <stdio.h>
#include "time.h"
#include <windows.h>

void clearScreen();
void getCurrentDate(char* buffer, size_t size);
void clearInputBuffer();
#endif
